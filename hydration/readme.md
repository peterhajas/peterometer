# Hydration Parsing Scripts
## `waterminder_parse.py`
This script parses data from [WaterMinder](http://waterminder.com/). It produces a stacked area plot with a legend in a file called "`hydration.png`" in the current directory.
